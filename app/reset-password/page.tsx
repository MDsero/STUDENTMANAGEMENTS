'use client'; import {useState} from 'react'; import {supabase} from '@/lib/supabase';
export default function Reset(){const[p,setP]=useState(''),[m,setM]=useState('');return <main className="min-h-screen grid place-items-center p-4"><form className="card w-full max-w-sm" onSubmit={async e=>{e.preventDefault();const{error}=await supabase.auth.updateUser({password:p});setM(error?error.message:'Password updated. You may sign in now.')}}><h1 className="text-xl font-bold">Set new password</h1><label className="field mt-4">New password<input type="password" minLength={8} required value={p} onChange={e=>setP(e.target.value)}/></label><button className="btn">Save password</button><p>{m}</p></form></main>}
