import {ResourcePage} from '@/components/resource-page'; export default function Page(){return <ResourcePage title="Announcements" table="announcements" columns={['title','body','published','created_at']} fields={[{key:'title',label:'Title',required:true},{key:'body',label:'Message',type:'textarea',required:true},{key:'published',label:'Published',type:'select',options:['true','false']}]}/>}
