import {Stack} from 'expo-router'; import {QueryClient,QueryClientProvider} from '@tanstack/react-query'; import {useState} from 'react';
export default function Layout(){const[c]=useState(()=>new QueryClient({defaultOptions:{queries:{staleTime:30000,retry:2}}}));return <QueryClientProvider client={c}><Stack screenOptions={{headerStyle:{backgroundColor:'#1e40af'},headerTintColor:'#fff'}}><Stack.Screen name="index" options={{title:'RRAcademy'}}/><Stack.Screen name="login" options={{title:'Sign in'}}/><Stack.Screen name="(tabs)" options={{headerShown:false}}/></Stack></QueryClientProvider>}
