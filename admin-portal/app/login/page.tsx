'use client'; import {useState} from 'react'; import {supabase} from '@/lib/supabase'; import {useRouter} from 'next/navigation';
export default function Login(){const r=useRouter(),[email,setEmail]=useState(''),[password,setPassword]=useState(''),[msg,setMsg]=useState(''),[busy,setBusy]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();setBusy(true);const {error}=await supabase.auth.signInWithPassword({email,password});setBusy(false);if(error)setMsg(error.message);else r.replace('/dashboard')} async function reset(){if(!email)return setMsg('Enter your email first.');const {error}=await supabase.auth.resetPasswordForEmail(email,{redirectTo:`${location.origin}/reset-password`});setMsg(error?error.message:'Password reset email sent.')}return <main className="min-h-screen grid place-items-center p-4"><form onSubmit={submit} className="card w-full max-w-sm"><h1 className="text-2xl font-bold">RRAcademy</h1><p className="muted mb-5">Administrator sign in</p><label className="field">Email<input required type="email" value={email} onChange={e=>setEmail(e.target.value)}/></label><label className="field">Password<input required type="password" value={password} onChange={e=>setPassword(e.target.value)}/></label>{msg&&<p className="error">{msg}</p>}<button disabled={busy} className="btn w-full">{busy?'Signing in…':'Sign in'}</button><button type="button" onClick={reset} className="btn btn-secondary w-full mt-3">Forgot password?</button></form></main>}
